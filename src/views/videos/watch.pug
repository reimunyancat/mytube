extends ../base

block content
    video(src="/" + video.fileUrl)
    div
        p=video.description
        small=video.createdAt
    div
        small Uploaded by #{owner.name}
    if video.owner.equals(loggedInUser._id)
        a(href=`${video.id}/edit`) Edit Video &rarr;
    br
    a(href=`${video.id}/delete`) Delete Video &rarr;